<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.classmates">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Classmates">
        <activity
            android:name=".screens.MenuActivity"
            android:label=""
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".screens.ShowClassmatesActivity"
            android:label="Одногруппники"
            android:parentActivityName=".screens.MenuActivity"/>

        <activity
            android:name=".screens.AddClassmateActivity"
            android:label="Добавить одногруппника"
            android:parentActivityName=".screens.MenuActivity"/>

        <activity
            android:name=".screens.UpdateClassmateActivity"
            android:label="Редакция одногруппника"
            android:parentActivityName=".screens.MenuActivity"/>

    </application>

</manifest>